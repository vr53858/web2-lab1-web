<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ticket App - Home</title>
</head>
<body>
    <h1>Welcome to the Ticket App</h1>
    <p>There are currently <strong><%= totalTickets %></strong> tickets in the system.</p>
    <p>To view a ticket, go to /ticket/:id where <strong>id</strong> is the ticket ID.</p>
    <% if (!isLoggedIn) { %>
        <button onclick="location.href='/login'">Log In</button>
    <% } else { %>
        <button onclick="location.href='/logout'">Log Out</button>
    <% } %>
</body>
</html>